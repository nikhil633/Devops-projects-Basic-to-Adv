# NOTE: In production use Azure Key Vault + CSI driver or External Secrets Operator
# This is a placeholder — never commit real secrets to Git
apiVersion: v1
kind: Secret
metadata:
  name: product-api-secret
  namespace: product-api
type: Opaque
data:
  DB_USER: dXNlcg==         # base64: user
  DB_PASSWORD: cGFzc3dvcmQ= # base64: password
